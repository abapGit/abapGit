{
  "name": "abapgit-giteatest",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "",
  "scripts": {
    "echo": "echo Gitea running on http://localhost:3050 user:abapgit password:abapgit",
    "gitea": "docker compose down && docker compose up -d && npx playwright install && playwright test && sleep 20 && sh create.sh && npm run echo"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^22.18.6"
  }
}
